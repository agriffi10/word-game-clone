VITE_API_BASE_URL=
# Required — the app's Supabase client throws at load if these are unset.
# Any valid-format URL/key works for local dev and e2e (guess validation is
# stubbed in tests); real values only matter against a live Supabase.
VITE_SUPABASE_DATABASE_URL=
VITE_SUPABASE_ANON_KEY=